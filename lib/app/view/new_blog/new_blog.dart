import 'package:flutter/cupertino.dart';

class NewBlogView extends StatelessWidget {
  /// Creates new blog  by the logged in user
  const NewBlogView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
